CREATE PROC AddAnimal (
@rfid varchar(30),
@crateNo int,
@vetPriority int,
@type varchar(30),
@name varchar(30),
@dob date,
@sex char(1),
weight DECIMAL(4,2),
diseases VARCHAR(50),
allergies VARCHAR(50),
notes VARCHAR(200)
)
AS

/*needs completion*/

